# 페이지 확인 체크리스트

## 📋 구현된 페이지 목록

### 1. 공개 페이지 (인증 불필요)
- [x] **루트 페이지** (`/`)
  - URL: `http://localhost:3000/`
  - 기능: 인증 상태에 따라 `/dashboard` 또는 `/login`으로 리디렉션
  - 확인 사항: 로그인 전/후 리디렉션 동작 확인

- [x] **로그인 페이지** (`/login`)
  - URL: `http://localhost:3000/login`
  - 기능: 이메일/비밀번호 로그인, OAuth 로그인
  - 확인 사항:
    - [x] 로그인 폼 표시
    - [x] 로그인 성공 시 `/dashboard`로 리디렉션
    - [x] OAuth 버튼 표시
    - [x] 회원가입 링크 동작

- [x] **회원가입 페이지** (`/signup`)
  - URL: `http://localhost:3000/signup`
  - 기능: 신규 사용자 회원가입
  - 확인 사항:
    - [x] 회원가입 폼 표시
    - [x] 회원가입 성공 시 처리

### 2. 사용자 페이지 (인증 필요 - Tier 1)

- [x] **대시보드** (`/dashboard`)
  - URL: `http://localhost:3000/dashboard`
  - 기능: 개인 대시보드, 모든 엔티티 통합 현황
  - 확인 사항:
    - [x] 사용자 이름 표시
    - [ ] 엔티티 선택기 (EntitySwitcher) 표시
    - [ ] 통계 카드 (출근, 근무시간, 조직 수, 센터 수)
    - [ ] 조직/센터 빠른 이동 버튼
    - [ ] 멤버십이 없을 때 Empty State 표시

- [ ] **설정 페이지** (`/settings`)
  - URL: `http://localhost:3000/settings`
  - 기능: 프로필, 계정, 외관, 개인정보 설정
  - 확인 사항:
    - [ ] 4개 탭 (Profile, Account, Appearance, Privacy) 표시
    - [ ] Profile 탭: 프로필 정보 조회/수정
    - [ ] Account 탭: 계정 정보 표시
    - [ ] Appearance 탭: 테마 토글 동작
    - [ ] Privacy 탭: 개인정보 설정 표시

- [ ] **조직 컨텍스트 페이지** (`/org/[orgId]`)
  - URL: `http://localhost:3000/org/{orgId}`
  - 기능: 특정 조직 컨텍스트에서 출퇴근/위치보고
  - 확인 사항:
    - [ ] Entity Context Banner 표시
    - [ ] 엔티티 선택기 (모바일)
    - [ ] 출퇴근 카드 (구현 예정 표시)
    - [ ] 위치보고 카드 (구현 예정 표시)
    - [ ] 현재 상태 카드 (구현 예정 표시)
    - [ ] 최근 기록 카드 (구현 예정 표시)
  - 주의: 실제 orgId가 필요 (대시보드에서 링크 클릭)

- [ ] **센터 컨텍스트 페이지** (`/center/[centerId]`)
  - URL: `http://localhost:3000/center/{centerId}`
  - 기능: 특정 센터 컨텍스트에서 출퇴근/위치보고
  - 확인 사항:
    - [ ] Entity Context Banner 표시
    - [ ] 엔티티 선택기 (모바일)
    - [ ] 출퇴근 카드 (구현 예정 표시)
    - [ ] 위치보고 카드 (구현 예정 표시)
    - [ ] 현재 상태 카드 (구현 예정 표시)
    - [ ] 최근 기록 카드 (구현 예정 표시)
  - 주의: 실제 centerId가 필요 (대시보드에서 링크 클릭)

## 🔍 확인 방법

### 1. 수동 확인 (권장)
브라우저에서 직접 접속하여 확인:

```bash
# 개발 서버가 실행 중이어야 함
# http://localhost:3000 에서 시작
```

### 2. 자동 확인 스크립트
터미널에서 실행:

```bash
# 모든 페이지의 HTTP 상태 코드 확인
./scripts/check-pages.sh
```

## 🐛 일반적인 문제 해결

### 인증 오류
- 로그인 후에도 리디렉션되는 경우: 세션 확인 필요
- 프로필이 없는 경우: `handle_new_user` 트리거 확인

### 404 오류
- `/org/[orgId]` 또는 `/center/[centerId]`: 실제 ID가 필요
- 대시보드에서 멤버십이 있는 엔티티로 이동

### 빈 화면
- 브라우저 콘솔 확인
- 서버 로그 확인 (`pnpm dev` 터미널)

## 📝 확인 순서 권장

1. **로그인 전**
   - `/` → `/login` 리디렉션 확인
   - `/login` 페이지 확인
   - `/signup` 페이지 확인

2. **로그인 후**
   - `/dashboard` 확인
   - `/settings` 확인
   - 대시보드에서 조직/센터 링크 클릭하여 동적 라우트 확인

3. **기능별 확인**
   - 프로필 수정 기능
   - 테마 변경 기능
   - 엔티티 전환 기능

